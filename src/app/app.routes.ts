import { Routes } from '@angular/router';

import { RestaurantListComponent } from './components/restaurant-list';

export const routes: Routes = [
    { path: '', component: RestaurantListComponent }
];
